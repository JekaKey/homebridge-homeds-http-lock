{
  "name": "homebridge-homeds-http-lock",
  "version": "0.0.1",
  "description": "HomeDS http lock",
  "keywords": [
    "homebridge-plugin"
  ],
  "engines": {
    "node": ">=0.12.0",
    "homebridge": ">=0.2.0"
  },
  "dependencies": {
    "request": "^2.65.0"
  }
}
